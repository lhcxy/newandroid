<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="120dp"
                android:layout_height="wrap_content"
    >

    <ImageView
        android:id="@+id/img"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:src="@mipmap/icon_auction_success"
        />

    <TextView
        android:id="@+id/btn_to_pay"
        android:layout_width="80dp"
        android:layout_height="24dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="90dp"
        android:background="@drawable/bg_btn_auction_pay"
        android:gravity="center"
        android:text="@string/immediate_pay"
        android:textColor="#fff"
        android:textSize="16sp"
        android:visibility="gone"
        />

    <RelativeLayout
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/group"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="90dp"
        android:background="@drawable/bg_gift_anim"
        android:visibility="gone"
        >

        <TextView
            android:id="@+id/uname"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_marginLeft="10dp"
            android:layout_marginTop="4dp"
            android:textColor="#fff"
            android:textSize="12sp"
            />

        <TextView
            android:id="@+id/text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/uname"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="3dp"
            android:text="@string/auction_last_price"
            android:textColor="#fff"
            android:textSize="12sp"
            />

        <com.yunbao.phonelive.custom.DrawableTextView
            android:id="@+id/most_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignTop="@id/text"
            android:layout_toRightOf="@id/text"
            android:drawablePadding="2dp"
            android:textColor="#fff"
            android:textSize="12sp"
            app:drawableSize="12dp"
            app:left_drawable="@mipmap/icon_gift_diamond"
            />

    </RelativeLayout>
</RelativeLayout>